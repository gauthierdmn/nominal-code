The following text is malformed JSON. Common issues include unescaped double quotes inside string values, trailing commas, and missing commas. Fix the syntax errors and output ONLY the corrected JSON.

{broken_json}
